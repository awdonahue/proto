# Setup

## Required
- Python 3.7
- Pipenv (https://docs.pipenv.org/en/latest/)

## Setup
- From project root run `pipenv run python main.py -h` to get program usage
- To decode a specific file, run `pipenv run python decode.py txnlog.dat`
